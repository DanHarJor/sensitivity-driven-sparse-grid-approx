from setuptools import setup, find_packages

setup(
    name="sg-lib",
    version="0.1.0",
    packages=find_packages(),  # This will include sg_lib and sg_lib.grid
    description="Surrogate grid library",
    author="Your Name",
)
